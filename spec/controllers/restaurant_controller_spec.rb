require 'rails_helper'

RSpec.describe RestaurantController, type: :controller do
    #describe "index" do
    #    it "should find a specific restaurant" do
    #        expect(@restaurant).to be_a_kind_of Hash
    #        expect(@restaurant["name"]).to be_a_kind_of String
    #    end
    #    it "should find at least one review" do
    #        expect(@reviews).to be_a_kind_of Hash
     #       expect(@reviews["reviews"]).to be_a_kind_of Array
    #        expect(@reviews["reviews"].length > 0).to eq true
    #    end
    #end
end